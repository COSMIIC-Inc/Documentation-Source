# Dependencies
# prefer that these are in, so that project can be fully downloaded and built may need to update them but not sure how
#/node_modules

# Production
/build

# Dependencies
/node_modules

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
